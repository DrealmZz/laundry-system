const express = require('express');
const router = express.Router();

// Placeholder for report routes
router.get('/finance', (req, res) => res.json({ message: 'Finance report' }));

module.exports = router;
