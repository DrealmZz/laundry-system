const express = require('express');
const router = express.Router();

// Placeholder for transaction routes
router.post('/payments', (req, res) => res.json({ message: 'Process payment' }));

module.exports = router;
