const express = require('express');
const router = express.Router();

// Placeholder for laundry service routes
router.get('/', (req, res) => res.json({ message: 'Service list' }));

module.exports = router;
